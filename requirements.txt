# Python deps for the Hackagotchi host tooling — the CLI (host/hackagotchi_ctl.py) and the
# HIL test suites (firmware/c/tests/**). Install into a venv via ./setup-venv.sh (or:
# python3 -m venv .venv && .venv/bin/pip install -r requirements.txt).
#
# Pure host-side: nothing here touches the device firmware; these just drive the USB-CDC ports.

pyserial==3.5          # the USB-CDC transport for every host script (control + bridge ports)
Pillow>=11,<12         # OPTIONAL: only `hackagotchi_ctl.py shot` (OLED screenshot -> PNG) uses it;
                       # without it `shot` falls back to a dependency-free .pgm. Safe to omit.
